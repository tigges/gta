---
/**
 * /gta-v/economy
 * GTA V Story Mode economy page — mixed market + capital market (BAWSAQ).
 */
import Base from "../../layouts/Base.astro";
import ShareDropdown from "../../components/ShareDropdown.astro";
import CircularEconomy from "../../components/CircularEconomy.astro";
import ChartComplexityHtml from "../../components/ChartComplexityHtml.astro";
import CardFooter from "../../components/CardFooter.astro";
import GamePill from "../../components/GamePill.astro";
import SectionHeader from "../../components/SectionHeader.astro";

import economyData  from "../../../data/franchise/economy-models.json";
import stocksData   from "../../../data/gta-5/economy/assassination-stocks.json";
import missionsData from "../../../data/gta-5/missions/story-missions.json";
import heistsData   from "../../../data/gta-5/economy/online-heists.json";

const economy    = (economyData as any).economies.find((e: any) => e.title_id === "gta-5");
const guide      = (stocksData as any).guide_order as any[];
const maxProfit  = (stocksData as any).max_theoretical_profit_usd as number;
const missions   = (missionsData as any).missions as any[];
const heists     = (heistsData as any).heists as any[];
const bigScore   = heists.find((h: any) => h.id === "the-big-score");
const bestCut    = bigScore?.approaches?.[0]?.player_max_cut ?? 41600000;
---

<Base
  titleContext="gta-v"
  ogPage="gta-v-eco"
  title="GTA V Economy — Mixed Market + BAWSAQ Capital"
  description="GTA V Story Mode economy analysis: BAWSAQ mission-linked capital market, Lester assassination stocks, property passive income, and the complete $2B money strategy."
  crossLinksContext="gta-v"
>
  <!-- Header — image banner -->
  <section class="relative border-b border-border-dim overflow-hidden" style="background:var(--c-bg-deep)">
    <div class="absolute inset-0">
      <img src="/assets/gta5/characters/Trevorfranklinmichael-GTAV.webp" alt="" aria-hidden="true"
           class="w-full h-full object-cover" style="object-position:center 28%" loading="eager" />
      <div class="absolute inset-0" style="background:linear-gradient(to right, var(--c-bg-deep) 0%, color-mix(in srgb,var(--c-bg-deep) 88%,transparent) 40%, color-mix(in srgb,var(--c-bg-deep) 52%,transparent) 72%, color-mix(in srgb,var(--c-bg-deep) 34%,transparent) 100%)"></div>
      <div class="absolute inset-0" style="background:linear-gradient(to bottom, color-mix(in srgb,var(--c-bg-deep) 42%,transparent) 0%, transparent 28%, transparent 58%, var(--c-bg-deep) 100%)"></div>
      <div class="absolute inset-0 pointer-events-none" style="background:radial-gradient(70% 90% at 85% 0%, var(--c-hero-bloom), transparent 70%)"></div>
    </div>
    <div class="relative max-w-7xl mx-auto px-4 sm:px-6 py-14">
      <div class="flex items-center gap-2 mb-4">
        <GamePill game="gta-v" size="xs" />
      </div>
      <h1 class="text-3xl sm:text-5xl font-bold text-white mb-2 leading-[1.05]">GTA V Story — Economy</h1>
      <p class="text-zinc-300 text-sm leading-relaxed mb-6 max-w-xl">
        The franchise's first capital market. BAWSAQ lets player actions propagate through stock prices.
        Combined with property passive income, compounding wealth is possible for the first time.
      </p>
      <div class="grid grid-cols-2 sm:grid-cols-4 gap-3 max-w-2xl">
        {[
          { value: "$41.6M",  label: "Big Score max cut",    sub: "optimal crew · subtle" },
          { value: "$2B+",    label: "assassination ceiling", sub: "all 5 missions combined" },
          { value: `${guide.length}`,     label: "BAWSAQ missions",      sub: "mission-linked stocks" },
          { value: "9",       label: "complexity score",     sub: "out of 20 max" },
        ].map(s => (
          <div class="rounded-xl border border-border-dim px-4 py-3" style="background:var(--c-card-raised)">
            <div class="text-xl font-mono font-black text-gta-gold">{s.value}</div>
            <div class="text-[11px] text-zinc-500 mt-0.5 leading-snug">{s.label}</div>
            <div class="text-[10px] font-mono text-zinc-400 mt-0.5">{s.sub}</div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <div class="max-w-7xl mx-auto px-4 sm:px-6 py-12 flex flex-col gap-12">

    <!-- Circular Economy Diagram -->
    <section>
      <SectionHeader bright label="// Circular Economy Model" />
      <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
        <div class="lg:col-span-2 rounded-xl border border-border-dim p-5" style="background:var(--c-bg)">
          <CircularEconomy titleId="gta-5" mode="full" />
          <CardFooter anchorId="eco-gta-v" sources="Source: curated · GTA Fandom Wiki · franchise economic analysis" shareText="GTA V's economy: capital market model — missions, stock assassinations, heists. The $2B strategy is the most sophisticated single-player money system in GTA history." />
        </div>
        <ChartComplexityHtml titleId="gta-5" showAverage={true} />
      </div>
    </section>

    <!-- BAWSAQ Mission Stock Guide -->
    <section>
      <div class="flex items-center gap-3 mb-6">
        <span class="text-[11px] font-mono px-2 py-0.5 rounded uppercase tracking-widest" style="color:var(--c-chart-purple);background:color-mix(in srgb,var(--c-chart-purple) 14%,transparent);border:1px solid color-mix(in srgb,var(--c-chart-purple) 35%,transparent)">Capital Market</span>
        <span class="text-[10px] font-mono text-zinc-100 tracking-[0.2em] uppercase">// BAWSAQ — Assassination Mission Returns</span>
        <div class="flex-1 h-px bg-border-dim"></div>
        <a href="/gta-online/assassination-guide" class="text-[10px] font-mono text-zinc-500 hover:text-zinc-400 transition-colors tracking-widest uppercase">Full guide →</a>
      </div>
      <div class="rounded-xl border border-border-dim overflow-hidden" style="background:var(--c-card-raised)">
        <div class="px-5 py-3 border-b border-border-dim flex items-center justify-between">
          <div class="text-sm font-semibold text-zinc-200">
            The $2 Billion Strategy — Optimal Sequence
          </div>
          <div class="text-[10px] font-mono text-zinc-400">{stocksData.optimal_sequence_note}</div>
        </div>
        <div class="divide-y divide-border-dim">
          {guide.map((step: any, i: number) => {
            const pct = step.combined_return_pct;
            const color = pct >= 300 ? "#22c55e" : pct >= 100 ? "#f59e0b" : "#0d9488";
            const buy  = step.strategy?.find((s: any) => s.action === "buy");
            return (
              <div class="px-5 py-3 flex items-center gap-4">
                <div class="shrink-0 w-5 h-5 rounded-full bg-gta-gold/15 border border-gta-gold/30 flex items-center justify-center text-[11px] font-mono font-bold text-gta-gold">
                  {step.step}
                </div>
                <div class="flex-1 min-w-0">
                  <div class="text-[11px] font-semibold text-zinc-200">{step.title}</div>
                  {buy && <div class="text-[11px] font-mono text-zinc-400 mt-0.5">Buy: {buy.ticker} ({buy.exchange})</div>}
                </div>
                <div class="shrink-0 text-right">
                  <div class="text-base font-mono font-black" style={`color:${color}`}>+{pct}%</div>
                  <div class="text-[10px] font-mono text-zinc-500">combined</div>
                </div>
              </div>
            );
          })}
        </div>
        <div class="px-5 py-3 border-t border-border-dim flex items-center justify-between gap-3">
          <span class="text-[10px] font-mono text-zinc-500">Do Hotel Assassination first. Complete remaining 4 AFTER The Big Score. Reinvest between each.</span>
          <ShareDropdown anchorId="economy" shareText="GTA V assassination strategy: do all 5 missions after The Big Score. Multi-Target + Redwood = +380% alone. Full $2B guide on GTAVI.AI." size="sm" />
        </div>
      </div>
    </section>

    <!-- Key Innovation -->
    <section>
      <SectionHeader bright label="// Key Innovation" />
      <div class="rounded-xl border border-border-dim overflow-hidden" style="background:var(--c-card-raised);border-left:3px solid var(--c-brand)">
        <div class="px-6 py-5">
          <div class="text-[10px] font-mono uppercase tracking-widest mb-2 text-gta-gold">Capital Market Era — {economy.title_name}</div>
          <div class="text-base font-semibold text-zinc-200 mb-2">{economy.key_innovation}</div>
          <div class="text-[11px] font-mono text-zinc-500 leading-relaxed mb-4">{economy.economy_type} · {economy.real_world_analogue}</div>
          <div class="flex flex-wrap gap-2">
            {(Object.entries(economy.dimension_scores) as [string,number][])
              .filter(([,v]) => v > 0)
              .map(([key, val]) => (
              <span class="text-[10px] font-mono border border-border-mid text-zinc-500 px-2 py-0.5 rounded capitalize">
                {key.replace(/_/g, " ")} <span class="text-gta-gold">{val}/3</span>
              </span>
            ))}
          </div>
        </div>
      </div>
    </section>

    <!-- GTA VI Lineage -->
    <section class="rounded-xl border border-gta-gold/15 px-6 py-5" style="background:var(--c-hero-bloom)">
      <div class="text-[11px] font-mono text-gta-gold uppercase tracking-widest mb-2">GTA VI Lineage</div>
      <p class="text-[11px] font-mono text-zinc-300 leading-relaxed">{economy.gta_vi_lineage}</p>
    </section>

    <div class="flex items-center gap-3 flex-wrap">
      <a href="/franchise/economics" class="px-4 py-2 rounded border border-border-mid text-zinc-400 text-[11px] font-mono hover:border-zinc-600 hover:text-zinc-200 transition-all">← Ten Economies</a>
      <a href="/gta-v" class="px-4 py-2 rounded border border-border-mid text-zinc-400 text-[11px] font-mono hover:border-zinc-600 hover:text-zinc-200 transition-all">GTA V Story Mode →</a>
      <a href="/gta-online/economy" class="px-4 py-2 rounded border border-gta-teal/30 text-gta-teal text-[11px] font-mono hover:bg-gta-teal/20 transition-all">What came next: GTA Online →</a>
    </div>

  </div>
</Base>
